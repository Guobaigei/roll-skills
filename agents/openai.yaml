interface:
  display_name: "OpenClaw 安装"
  short_description: "Node + npm 安装 OpenClaw；Windows 走原生 PowerShell。"
  default_prompt: "使用 $openclaw-install 帮我在 macOS 或 Windows 上用 npm 安装 OpenClaw，Windows 走原生 PowerShell，并直接配置飞书和默认模型。"

policy:
  allow_implicit_invocation: true
