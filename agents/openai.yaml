interface:
  display_name: "OpenClaw 安装"
  short_description: "在 macOS / Windows 上安装并排障 OpenClaw。"
  default_prompt: "使用 $openclaw-install 帮我在 macOS 或 Windows 上安装或排查 OpenClaw。"

policy:
  allow_implicit_invocation: true
